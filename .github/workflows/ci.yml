name: CI

on:
  push:
    paths-ignore:
      - '.github/**'
      - 'README.md'
      - 'LICENSE'
      - 'NOTICE'
  pull_request:
    branches: [ main ]
    paths-ignore:
      - '.github/**'
      - 'README.md'
      - 'LICENSE'
      - 'NOTICE'
  workflow_dispatch:

# Cancel a run superseded by a newer one for the same ref, e.g. a second push to a branch before
# the first build finished. Push and pull_request runs deliberately stay in separate groups: a push
# run builds the branch head, a pull_request run builds the merge with the base branch, and letting
# either cancel the other could leave the merge untested.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1

      - name: Set up Java 21
        uses: actions/setup-java@v6.0.0
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build and test
        run: ./mvnw -q package

      - name: Upload jar artifact
        uses: actions/upload-artifact@v7.0.1
        with:
          name: java-buildpack-client-certificate-mapper
          path: |
            java-buildpack-client-certificate-mapper/target/*.jar
            !java-buildpack-client-certificate-mapper/target/*-sources.jar
            !java-buildpack-client-certificate-mapper/target/*-javadoc.jar
            !java-buildpack-client-certificate-mapper/target/original-*.jar
          if-no-files-found: error

  snapshot:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7.0.1

      - name: Set up Java 21
        uses: actions/setup-java@v6.0.0
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      # Only a -SNAPSHOT revision belongs in the rolling snapshot release; releases come from tags.
      - name: Check snapshot version
        id: version
        run: |
          VERSION=$(./mvnw help:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "Project version: ${VERSION}"
          if [[ "${VERSION}" == *-SNAPSHOT ]]; then
            echo "snapshot=true" >> "$GITHUB_OUTPUT"
          else
            echo "::notice::main is at ${VERSION}, not a -SNAPSHOT: skipping the rolling snapshot release."
          fi

      - name: Build
        if: steps.version.outputs.snapshot == 'true'
        run: ./mvnw -q package -DskipTests

      - name: Collect artifacts
        if: steps.version.outputs.snapshot == 'true'
        id: artifacts
        run: |
          JAR=$(ls java-buildpack-client-certificate-mapper/target/java-buildpack-client-certificate-mapper-*SNAPSHOT.jar \
                | grep -v 'sources\|javadoc\|original' | head -1)
          SOURCES=$(ls java-buildpack-client-certificate-mapper/target/java-buildpack-client-certificate-mapper-*SNAPSHOT-sources.jar \
                    | head -1)
          echo "jar=$JAR" >> $GITHUB_OUTPUT
          echo "sources=$SOURCES" >> $GITHUB_OUTPUT

      - name: Recreate rolling snapshot release
        if: steps.version.outputs.snapshot == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION=$(./mvnw help:evaluate -Dexpression=project.version -q -DforceStdout)
          gh release delete snapshot --yes 2>/dev/null || true
          git push origin :refs/tags/snapshot 2>/dev/null || true
          gh release create snapshot \
            --prerelease \
            --title "Latest SNAPSHOT ($VERSION)" \
            --notes "Automatically published on every push to \`main\`. Always reflects the latest snapshot build." \
            "${{ steps.artifacts.outputs.jar }}" \
            "${{ steps.artifacts.outputs.sources }}"
